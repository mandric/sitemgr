# Opus Review

**Model:** claude-opus-4
**Generated:** 2026-04-01

---

### Summary of Required Plan Amendments

**Must fix (will cause incomplete rename or broken tests):**

1. Add `scripts/setup/verify.sh` to Section 4 -- 5 references to `SMGR_API_URL`/`SMGR_API_KEY`.
2. Add `web/__tests__/unit/cli-auth-device-flow.test.ts` to Section 3 -- `SMGR_WEB_URL` stub and `SMGR_API_URL` in test description.
3. Add `web/__tests__/integration/device-auth.test.ts` to Section 3 -- `SMGR_WEB_URL` read on line 14.
4. Add `SMGR_OLLAMA_URL` and `SMGR_VISION_MODEL` to Section 4's CI workflow scope.
5. Explicitly enumerate `SMGR_API_URL` and `SMGR_API_KEY` in Sections 3 and 4 -- these are the most-referenced env vars and are currently implicit at best.
6. Fix the verification grep to cover `Dockerfile` at repo root, `CLAUDE.md`, and `.env.example`.

**Should fix (clarity and correctness):**

7. Warn about the substring hazard: `smgr` appears inside existing `sitemgr` strings. Replacement strategy must use word boundaries or targeted prefix matching, not blind `sed s/smgr/sitemgr/g`.
8. Note that `.env.local` is auto-regenerated by `scripts/lib.sh`, so existing local dev setups will self-heal on next `npm run setup:env`.
9. Make the CLAUDE.md updates explicit rather than conditional.
10. Clarify whether intermediate test runs between sections are expected, and if not, recommend implementing all sections as a single atomic commit.
