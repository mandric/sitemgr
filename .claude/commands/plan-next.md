# Plan Next

Find the next unimplemented spec in `specs/` and run the Autonomous Development Process from CLAUDE.md (Phase 0 through Phase 5).

If the user specified a spec, use that. Otherwise, pick the next numbered spec without an `implementation/` directory.
