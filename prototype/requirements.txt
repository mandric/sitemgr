# smgr prototype dependencies

# S3 watcher
boto3>=1.34.0

# Enrichment providers (install the one you use)
anthropic>=0.40.0    # Claude vision API
# openai>=1.0.0      # GPT-4o or Gemini vision API (optional, needed for openai/gemini providers)

# WhatsApp bot (optional — only needed for bot.py)
# twilio>=9.0.0      # Twilio WhatsApp API (optional, bot uses urllib fallback)
