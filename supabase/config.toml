[api]
enabled = true
port = 54321
schemas = ["public", "storage"]

[db]
port = 54322
major_version = 15

[analytics]
enabled = false  # Disable analytics (causes issues with Colima Docker socket mounting)

[functions.whatsapp]
verify_jwt = false  # Twilio webhooks don't send JWTs
